# Проверка реализации Firebird

## Исправления better-interface — 23.09.2026, 17:52 Алматы

Проверен код в том же коммите, что и этот раздел: исправления после аудита main `6b98220`. macOS, Node.js 22.22.2, npm 10.9.7, исходные 66 профилей. Старая история проверок ниже относится к прежним версиям.

- **357 unit/integration тестов** в 13 файлах проходят. Покрытие ядра/API: строки **99,37%**, statements **99,29%**, branches **96,92%**, functions **100%**. Эти проценты не относятся к UI.
- Новые регрессии сначала падали: сетевой текст, пустой заголовок, структурированные действия подсказок, общий блок условий, е/ё и отрицания. Исправления проверяют сохранение прочих условий при смене бюджета/даты/города и различимость объяснений.
- `npm run build`, `npm run typecheck`, `git diff --check` прошли. `npm audit`: 0 известных уязвимостей.
- На финальной production-сборке (`npm start -- --port 3100`) прошли **11 браузерных сценариев**: 6 из `npm run test:e2e`, 5 из `npx playwright test --config components/playwright.config.ts`.
- Браузер: начальный экран и выдача 1440px, результат 320px без горизонтального переполнения. Дата/бюджет, сравнение двух дат, перезапрос после сбоя, сохранение полей и storage failure проверены сценариями. Скриншот README обновлён с production-сервера.
- axe-core: теги wcag2a/wcag2aa/wcag21aa/wcag22aa — **0 нарушений на проверенных начальном экране и популярной выдаче**. Это не сертификат доступности всех состояний и устройств.
- Независимый обзор diff нашёл ошибочную область отрицания в перечислении и предложной группе. Исправлены примеры «Без шаблонов и банальных конкурсов» и «без сахара с индивидуальным дизайном», добавлены регрессии; повторный обзор подтвердил закрытие замечания.
- Запрос «тихий формальный вечер» больше не повышает кандидата за прямой отказ; конфликт, если кандидат всё же показан, обозначается в объяснении. Это ограниченная лексическая эвристика, не полное понимание естественного языка.

Не проверены: реальный VoiceOver/NVDA, физический iPhone, браузерный zoom 200%, публичный хостинг и конкурентная нагрузка. Внешние API/ключи/платные сервисы не добавлялись.


23.09.2026, время Астаны (UTC+5).

## Качество объяснений без изменения UI — 17:10

Функциональный коммит `c2d9dd6`. UI, CSV, фильтры и ранжирование не изменены.

- Вместо общего текста о более высокой цене/отсутствии преимущества показываем проверяемый контраст по длительности либо фактические условия. Для Эмилии: до 10 часов против максимум 8 у более дешёвых показанных профилей.
- Отбор цитат учитывает опыт, специализацию, состав/репертуар, оборудование; вступления и реклама понижают оценку. Лимит 140 символов сохраняет целую фразу «Опыт ведения свадеб 13 лет». При отсутствии положительно оценённого фрагмента цитата опускается, структурные факты остаются. Это эвристика, не внешняя проверка описаний.
- Исправлены согласование «1 профиль / 2 профиля / 5 профилей» и повтор текста при редкой категории без отсева.
- `npm run test:coverage`: **340/340**; statements **99,21%**, branches **97,01%**, functions **100%**, lines **99,30%** (lib/API). Регрессии сначала воспроизводили дефекты. Перебор 11 200 запросов проверяет различимость без имён и принадлежность всех включённых цитат источнику; отсутствие цитаты допускается для слабого текста при сохранении цены, формата и даты.
- Независимая приёмка: 229 объяснений на исходных 66 профилях, 112 сочетаниях на 15 октября и демо; 0 цитат вне источника, 0 дублей без имён, 0 объяснений длиннее двух предложений. Независимый обзор кода и безопасности не нашёл блокирующих замечаний. Проверка не охватывает все возможные новые данные.
- `typecheck`, `git diff --check`, `npm audit` прошли; 0 известных уязвимостей.
- Чистый клон `/tmp/firebird-explanations-clean-1708`, созданный `git clone --no-local`: `npm ci` → 340 тестов → 3 E2E → production build, все с кодом 0. Без переноса `.env`, `.next` и `node_modules`.
- Запуск готовой сборки через `npm start` на 3111: все пять fixtures совпали по исходам и ID; сравнение 3→1 с двумя причинами `busy`; повторный HTTP-запрос даёт идентичный ответ. Ответы пяти fixtures 2,2–51,0 мс, сравнение 4,1 мс на этой машине; это короткая локальная проверка, не новая нагрузочная гарантия.

## Техническое ядро — 16:43

Функциональная версия: `6940823`. Приоритет пользователя — шесть требований ТЗ и объяснения; большой нагрузочный стенд признан дополнительным, дальнейшие сценарии не запускаются.

- Потоковый импорт создаёт единый неизменяемый снимок с индексами по городу/категории и ID. Подготовка выполняется до готовности сервера; обновление CSV требует перезапуска.
- Валидация, подбор и обе даты сравнения используют один снимок. Отбор top-3 сохраняет прежний порядок, подсказки не передают большие массивы в `Math.min`.
- Исправлены подтверждённые ошибки импорта: пустая цена больше не становится нулевой; неверная занятая дата отклоняет весь каталог, а не делает профиль доступным. Есть проверки атомарного отказа и повторной загрузки исправленного файла.
- `npm run test:coverage`: 320/320, statements 98,90%, branches 96,15%, functions 100%, lines 99,26% (lib/API). Включены 216 сочетаний запросов и каталогов против независимого эталона старого алгоритма: порядок, объяснения, исходы, последовательные счётчики, площадки, опциональные условия и детерминизм при перестановке входа.
- `npm run typecheck`, `git diff --check`, `npm audit` — успешно; известных уязвимостей 0. Независимый обзор подтвердил исправление двух ошибок импорта и отсутствие блокирующих замечаний.
- Чистый клон `/tmp/firebird-core-clean-1643` через `git clone --no-local`: `npm ci` → 320 тестов → 3 E2E → production build, все команды завершились с кодом 0. Исходный CSV не изменён.
- Production-сервер чистого клона (`npm start`, порт 3111): пять запросов из fixtures совпали по исходам и ID; сравнение дат вернуло 3→1 и реальные причины `busy`. Измеренный HTTP-ответ на исходном каталоге: 3–125 мс в этой короткой локальной проверке; не нагрузочная гарантия.

### Дополнительный эксперимент: найден предел, не сертификат масштаба

Apple M2, macOS arm64, Node.js 22.22.2, 8 ГБ RAM. Сгенерирован настоящий CSV с независимыми ID, описаниями и календарями; все записи в одном городе/категории. Данные временные, не входят в каталог продукта. Измерялись обработчики с валидацией и JSON, без сети, Next.js и браузера; конкурентный HTTP не проверялся. Стенд выполнялся до финального усиления проверки цен/дат при импорте; логика тёплого подбора после него не менялась.

| Показатель | 100 000 строк (2 повтора) | 1 000 000 строк (5 повторов) |
| --- | ---: | ---: |
| CSV | 166 МБ | 1,664 ГБ |
| Холодная загрузка | 4,33 с | 48,35 с |
| Максимум подбора | 0,481 с | 11,482 с |
| Максимум сравнения двух дат | 0,704 с | 16,179 с |

На миллионе установлен потолок heap 3072 МиБ; пиковый RSS процесса около 2,58 ГБ. Функциональные проверки этого эксперимента прошли, но проверка максимума <10 с **упала**. Не заявлять гарантированную работу за 10 с на миллионе или при любом объёме. Следующие нагрузочные прогоны отложены по приоритету пользователя; исходные шесть требований проверяются на реальном каталоге. Логи вне Git: `/tmp/firebird-bench-100k.log`, `/tmp/firebird-bench-million-dense.log`.

## Полировка объяснений и демо — 15:56

Проверяемый функциональный коммит: `8b8f3d0`. Объяснения начинаются с отличительного факта; цитаты до 80 символов, с приоритетом кратких содержательных фрагментов. Итог сравнения дат расположен перед карточками; подробный отсев раскрывается по нажатию.

- `npm run test:coverage`: 64/64 теста; statements 98,17%, branches 95,86%, functions 98,80%, lines 98,08% (ядро/API, не UI).
- Новая регрессия сначала падала на прежнем порядке объяснения, затем прошла. Полный перебор 11 200 запросов по сочетаниям каталога и 100 дням проверяет различимость текста и точность цитат. Независимый обзор подтвердил отсутствие одинаковых цитат внутри выдачи на этих же сочетаниях. Опциональные поля и все бюджеты этим перебором не покрыты.
- `npm run typecheck`, `git diff --check`, независимый обзор diff — без блокирующих замечаний; `npm audit` — 0 известных уязвимостей.
- Новый чистый клон `/tmp/firebird-polish-clean-1555`, созданный через `git clone --no-local`: `npm ci`, `npm test` (64/64), `npm run test:e2e` (3/3), `npm run build` завершились с кодом 0. Проверка на macOS; пользовательский dev-сервер в основной папке продолжал работать отдельно.
- В браузере просмотрен новый итог сравнения на desktop и mobile 390 px. Скриншоты вне Git: `/tmp/firebird-polished-compare.png`, `/tmp/firebird-polished-mobile.png`.
- Сценарий защиты и ответы на вопросы: [demo.md](demo.md). Формальная отправка на платформу этим прогоном не подтверждается.

Ниже сохранена история предыдущих проверок.

- Приёмочные тесты сначала проверены на функциональных заглушках: 23 падения ядра и 24 падения API, затем реализован рабочий код.
- `npm test`: 62 теста в четырёх файлах проходят.
- `npm run typecheck`: проходит.
- `npm run test:e2e`: три сценария Chromium проходят, включая три демо, повтор, сравнение дат и мобильную ширину.
- Браузерный прогон обнаружил несовпадение Origin из-за нормализации внутреннего URL в Next.js. Исправление защищено регрессионным тестом.
- `npm audit`: 0 известных уязвимостей на момент проверки.
- Независимый обзор ядра: 11 200 сочетаний запроса (112 сочетаний города/категории/формата ×100 дней, бюджет 1 млрд, без опциональных условий), 12 452 карточки. Нет одинаковых объяснений внутри выдачи, цитат вне исходного текста или объяснений длиннее двух предложений. Это не исчерпывающий перебор всех бюджетов/языков/длительностей.

## Чистый клон — пройден

Проверяемый код: `1d852f6` (после него меняется только отчёт проверки). 23.09.2026, 15:37–15:38 UTC+5. macOS, Node.js 22.22.2, npm 10.9.7.

Создан отдельный клон из Git-объектов локального командного репозитория с `git clone --no-local`, без переноса `node_modules`, `.next`, `.env` и незакоммиченных файлов. Каталог проверки: `/tmp/firebird-final-clean`. Это проверка содержимого коммитов; доступ к приватному GitHub жюри предоставляется организатором.

Последовательно выполнены:

```bash
npm i
npm test
npm run test:e2e
npm run build
```

Все команды завершились с кодом 0. Установка: 81 пакет, аудит 0 уязвимостей. Тесты: 62/62. E2E: 3/3, запускают `npm run dev -- --port 3100`, проверяют три демо, повтор выдачи, сравнение дат и мобильную ширину. HTTP-проверка ответа заняла 15 мс после запуска сервера (локальное наблюдение, не гарантия для любого оборудования). Production-сборка завершена, страница `/` и три API-маршрута созданы.

Предыдущий чистый клон `e0b97e2` дополнительно запущен через `npm start -- --port 3103`; браузер нажал три демо-кнопки production-сборки. Все получили HTTP200, совпали с fixtures: dense→3, rare→2, none_pass→0. Следующий коммит `1d852f6` меняет только прокрутку к результатам, доступное объявление и favicon; его полный clean-clone прогон указан выше.

## Покрытие и визуальная проверка

`npm run test:coverage`: statements97,26%, branches95,17%, functions97,61%, lines97,45%. Область измерения — `lib/` и `app/api/`, без файла типов; UI проверяется браузерными сценариями. Порог 80% выполнен по всем четырём метрикам.

Визуально просмотрены desktop1440px, mobile390px и сравнение дат. На мобильном нет горизонтального переполнения; после подбора фокус и прокрутка переводят к результатам. Проверена версия с финальным исправлением прокрутки.

`AGENTS.md` и `CLAUDE.md` одинаковые. Исходный CSV не изменён. Секреты, зависимости и артефакты сборки не включены в Git.

## Ограничения доказательств

Прогон выполнен на macOS; Linux/Windows и размещение за HTTPS reverse proxy отдельно не проверялись. Работа без ключей проверена, модельного AI-инференса нет. Срок ответа измерялся на локальном оборудовании, без нагрузочного тестирования. Будущие почасовые push и объявления закрытого чата этот отчёт не подтверждает.
