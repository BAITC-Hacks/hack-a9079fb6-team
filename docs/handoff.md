# Передача контекста: Firebird #79-lite

## Актуализация 23.09.2026, 15:56 UTC+5

Продолжение выполнено в правильной рабочей папке. При входе `main` совпадал с `origin/main` на `130624c`, дерево было чистым; демо на порту 3000 работало. Полировка в ветке `codex/firebird-demo-polish`, функциональный коммит `8b8f3d0`:

- Отличительный факт перенесён в начало объяснения; цитаты сокращены до 80 символов с сохранением атрибуции и точного фрагмента источника.
- Итог изменения дат поднят над карточками; воронка свёрнута; метка карточки уточнена до «Условия: N из M».
- Подготовлен [сценарий защиты на 2:30](demo.md). Слайды и видео не создавались.
- 64 теста, 3 E2E, typecheck, production build и новый чистый клон прошли. Подробности и пределы проверки: [verification.md](verification.md).
- Полный редизайн не выполнялся. Замечаний пользователя по ручной проверке пока нет; независимый запуск человеком также не подтверждён.

Следующий шаг: ручная репетиция по `demo.md`, замечания команды, проверка отдельных требований платформы к отправке. Jev запрещён; после 17:40 новых фич нет, финальный push до 17:55. Перед следующей работой снова проверить Git: статус публикации определяется сервером, а не сохранённым описанием.

Ниже — исходный handoff до полировки; новые сведения выше заменяют его устаревшие пункты.

Срез: **23.09.2026, 15:45, Алматы/Астана UTC+5**. Этот документ — точка входа для нового чата Codex или Claude. Это краткое сохранение рабочих решений и состояния, а не дословная история переписки.

## Последнее сообщение пользователя и следующая задача

Пользователь открыл рабочее демо, спрашивает, является ли UI предварительным, что реализовывать дальше, и просит сохранить контекст перед новым чатом. Результаты его ручной проверки ещё не получены. **Текущий UI — рабочая первая версия, подключённая к настоящему ядру; финальное визуальное направление пользователь ещё не утвердил.**

Не начинать проект заново и не считать существующий UI моковым. Сначала прочитать этот файл, `AGENTS.md`, `README.md`, `docs/plan.md`, `docs/contract.md`, `docs/verification.md`; проверить Git и текущее приложение. Приоритет следующего этапа — качество объяснений и улучшение понятности интерфейса по пользовательской проверке, затем репетиция сдачи.

## Где работать

- Репозиторий команды: https://github.com/BAITC-Hacks/hack-a9079fb6-team . Приватный, ранее проверен доступ WRITE через gh.
- **Рабочая папка: `/Users/dos/Desktop/hack-a9079fb6-team`.** Ветка `main`, последний проверенный опубликованный функциональный результат `a47ccad` (включает код `1d852f6` и MVP `e0b97e2`). Коммит этого документа появится поверх него.
- Перед сохранением передачи рабочее дерево чистое; main совпадал с опубликованным результатом. Перепроверить перед новой работой: другие участники могут пушить.
- Ветка реализации `codex/firebird-mvp` сохранена. Новые изменения делать в `codex/...`, интегрировать обычным push в main без force.
- `/Users/dos/Projects/hackalem-firebird` — старый клон, не актуальная рабочая папка. `/Users/dos/Documents/ChatGPT/hackalem` ранее был недоступен по правам macOS.
- Демо: http://127.0.0.1:3000 . Сервер может пережить чат, но нельзя считать его запущенным без проверки. Запуск: `npm run dev` из рабочей папки.
- Личная память: `/Users/dos/.codex/memories/hackalem/MEMORY.md`; ранние записи исторические, последние разделы их заменяют.

## Решения и запреты

- Выбран кейс **«Умный подбор подрядчиков», Firebird #79-lite**. Остальные треки, топ-3 и Navier–Stokes — завершённые обсуждения, не активная задача.
- Последнее распределение: **Codex делает и ядро, и UI**. Ранняя схема «Claude ядро / сокомандник UI» заменена прямым указанием пользователя. Перед параллельными изменениями сверить текущие планы участников.
- **Jev отменён пользователем.** Не подключать даже офлайн и не возобновлять прежние пробы. Пункт про Jev в пересланном плане Claude устарел.
- В текущем продукте нет LLM, внешнего AI API, эмбеддингов или фактов, извлечённых моделью. Работа полностью локальная, без ключей. Шаблоны честно обозначены шаблонами.
- Наличие AI в разработке не означает AI-инференс в продукте. Отсутствие модельного слоя прозрачно отражено в README; это возможный риск оценки технической реализации, а не подтверждённый запрет или обязательная недостающая функция. Добавление другого AI-слоя отдельно обсудить, сохранив запуск без ключей и детерминизм.
- Brev ранее подключён, но GPU-инстансов для проекта не запускали; текущему решению GPU не нужен. Исторический баланс не считать текущим, не запускать платные ресурсы автоматически.
- Не использовать тяжёлые новые наборы скиллов ради процесса. При работе опираться на существующий план и реальные проверки.

## Что уже работает

Next.js 16.3.6 App Router, React 19.3, TypeScript, Vitest 5.0.1, csv-parse 7, Zod 4, Playwright. Node 22.12+, проверено 22.22.2. CSV — единственный каталог, без БД.

- `lib/contract.ts`: реализованный общий контракт; `docs/contract.md`: описание HTTP.
- CSV: 66 профилей, 17 категорий; 13 синтетических уже в наборе организатора. Данные не изменены. Календарь23.09–31.12.2026. Пустой max_hours→null, списки через `|`.
- Строгие фильтры: город+категория → занятость → формат → бюджет → часы → язык; последовательные счётчики отсева.
- Стабильное ранжирование: лексическая релевантность формату/пожеланию → цена → запрошенный язык → длительность → id. Не семантическая модель.
- До 3 карточек, фактические объяснения из структурных полей, сравнение соседних карточек и точная цитата профиля; одинаковый запрос даёт одинаковый текст/порядок.
- Пять исходов: matched, partial, no_category_in_city, none_pass, date_out_of_range. Цена «от», маркировка synthetic/imputed, без выдуманных процентов пригодности.
- GET /api/catalog, POST /api/match, POST /api/compare; Zod, ограничение тела, безопасные ошибки, базовый лимит запросов.
- UI: форма, опциональные поля, три демо, карточки, пустые состояния, funnel, подсказки, сравнение дат и причина исчезновения busy/ranking. Запросы отменяются, устаревший результат помечается; таймаут 10 с; адаптивность и фокус/прокрутка на результат.
- Подсказки: ближайшая дата с большим числом подходящих и минимальная прибавка бюджета, добавляющая кандидата. Остальные условия сохраняются.
- README полностью обновлён: запуск, архитектура, фактические демо, проверка объяснений, ограничения и сторонние компоненты.

## Доказательства проверки

Подробности: `docs/verification.md`.

- 62 unit/integration теста,3 браузерных E2E проходят.
- Покрытие lib/API:97.26%операторов,95.17%ветвей,97.61%функций,97.45%строк. UI не входит в эти проценты, покрывается E2E.
- typecheck/build проходят; npm audit 0 на момент проверки.
- Чистый Git-клон без node_modules/.next/.env: npm i → test → E2E → build успешно. Предыдущая сборка также проверена npm start и тремя демо в браузере.
- Независимый перебор11 200 запросов (112 сочетаний × 100 дней, большой бюджет, без опциональных фильтров):12 452 карточки, 0 повторяющихся объяснений внутри выдачи, 0 цитат вне источника, 0 объяснений длиннее 2 предложений. Не исчерпывающий перебор всех входов.
- Визуально проверены1440 px / 390 px, сравнение дат; горизонтального переполнения нет. Скриншоты вне Git: `/tmp/firebird-desktop-result.png`, `/tmp/firebird-mobile-viewport.png`, `/tmp/firebird-compare.png` (первый снят до удаления дубля текста результата).
- Исправлены реальные дефекты: одинаковые объяснения двух лайв-бэндов; Next-нормализация localhost ломала Origin-проверку для127.0.0.1. Есть регрессионные тесты.
- HTTPS reverse proxy, Linux/Windows, высокая нагрузка отдельно не проверялись. Не запускать Next build и dev одновременно в одной папке: оба пишут .next.

## Ручная проверка, которую предложили пользователю

1. Популярная категория: Алматы/Ведущий/свадьба/15.10.2026/1млн→3карточки из5кандидатов:HK-35215,HK-42352,HK-44923.
2. Повтор→тот же порядок и текст.
3. Вторая дата19.12.2026→одинHK-35215; видно, кто занят.
4. Редкая категория: флористыАлматы15.10/300тыс→HK-90001,HK-39372 и причина отсутствия третьего.
5. Никто не подходит: банкетныйзалАлматы/той/26.12/1млн→none_pass, текст и подсказка.
6. Астана/Декоратор/свадьба/15.10/2млн→no_category_in_city.

Главный вопрос пользователю: помогают ли объяснения выбирать, различаются ли карточки после удаления имён? Пользователь ещё не прислал замечания или результат независимого запуска сокомандником.

## Что делать дальше по приоритету

Это предложения следующего этапа, не утверждение о уже найденных дефектах и не разрешение на неограниченный редизайн.

1. **Пользовательская проверка и объяснения.** Собрать замечания; улучшить читаемость, выделение отличительного факта и полезность цитат. Сохранить точность,1–2 предложения, детерминизм и отсутствие занятых. Не заменять доказательства рекламным текстом.
2. **Довести UI до финального демо.** Согласовать желаемый визуальный уровень/референс в новом чате; полировать иерархию, компактность карточек и сравнение дат. Это улучшение готового интерфейса, не замена моков реальным API.
3. **Подготовить защиту.** Короткий сценарий на 2–3 минуты: проблема→плотнаякатегория→контраст объяснений→втораядата→редкая/пустая→pipeline/воспроизводимость. Питч и слайды пока не созданы.
4. **Независимая приёмка.** Сокомандник запускает из GitHub по README. После изменений повторить затронутые тесты, финальный clean-clone прогон и обновить доказательства.
5. **Сдача.** Проверить отдельные инструкции платформы/закрытого чата по кнопке отправки и материалам. Публичное Положение прочитано; закрытые объявления и факт формальной отправки решения не проверены. Развёртывание в облаке пока не делали и обязательность не установлена.

## Правила и сроки

Источник: `docs/task.md`, `docs/rules-verification.md`; публичное Положение https://edu.astanahub.com/hackathons/df4743f5-c492-415c-b45a-1f13adb78e06?tab=regulations .

Оценка: работоспособность 25, техника 25, README/воспроизводимость 25, ценность 15, оригинальность 10. Приоритет кейса: объяснения→честные редкие/пустые результаты→скорость→UI. Независимый запуск обязателен; невозможность запуска по инструкции может означать недопуск без дальнейших исправлений.

Нужны реальные подтверждения каждого часа разработки. Буквального требования push именно в main в публичном тексте нет; это практика команды. Публикация текущего результата main выполнена около 15:38. В Git есть 14–15 и 15–16; за 13–14 коммита не видно. Пользователь уведомлён сохранить другие реальные материалы за этот час. Не фабриковать и не датировать историю задним числом.

**После 17:40 — новых фич нет; финальный push до 17:55; официальный дедлайн 18:00.** Не откладывать реальные почасовые результаты. Автоматические будущие push/напоминания не настроены. Новый чат должен заново проверить текущее время; не считать 15:45 текущим навсегда.
